#Докер файл используется для создания Докер-образа приложения
FROM python:3.11-slim
#Берем облегченную версия питона за основу

WORKDIR /app
  #создаем папку приложения, разворачиваем зависимости и устанавливаем их

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY ./app ./app

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
#Открываем Консоль при запуске: с новыми параметрами и перезагрузкой при изменении кода.